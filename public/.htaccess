RewriteEngine On

# Force the preferred canonical host and HTTPS in a single redirect.
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} ^www\.sitesprofissionai\.com$ [NC]
RewriteRule ^ https://sitesprofissionai.com%{REQUEST_URI} [R=301,L]